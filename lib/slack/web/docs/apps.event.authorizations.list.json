{
  "desc": "Returns a list of authorizations for the given event",
  "args": {
    "token": {
      "description": "App-Level Authentication token. Requires scope: `authorizations:read`",
      "required": true,
      "type": "string"
    },
    "event_context": {
      "description": "The `event-context` which is extracted from an Event payload"
      "required": true,
      "type": "string"
    },
    "cursor": {
      "required": false,
      "type": "string",
      "desc": "Paginate through collections of data by setting the cursor parameter to a `next_cursor` attribute returned by a previous request's `response_metadata`. Default value fetches the first \"page\" of the collection. See pagination for more detail."
    },
    "limit": {
      "required": false,
      "type": "integer",
      "desc": "The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the list hasn't been reached. Must be an integer no larger than 1000.",
    }
  },
  "response": {
    "ok": true,
    "authorizations": {
      "enterprise_id": "string",
      "is_bot": "string",
      "team_id": "string",
      "user_id": "string"
    }
  },
  "errors": {
    "invalid_event_context": "The given `event_context` didn't match an event.",
    "not_allowed_token_type": "The token type used in this request is not allowed.",
    "invalid_cursor": "The `cursor` argument was invalid."
  }
}
